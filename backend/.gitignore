# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Env & secrets — never in git
.env
.env.*
!.env.example
ops/tls/out/

# Agent-facing docs — kept local, not in version control
ARCHITECTURE.md
CLAUDE.md

# Runtime artifacts
media_root/
static_root/
*.log

# Test & tooling caches
.pytest_cache/
.coverage
htmlcov/

# OS noise and stray local databases
.DS_Store
*.sqlite3

# Vendored wheels are LARGE but are the offline lifeline. They are intentionally NOT
# ignored: the human decides whether to commit them or store them elsewhere. Keep this
# comment; do not add vendor/ below.
