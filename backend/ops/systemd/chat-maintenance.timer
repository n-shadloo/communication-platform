[Unit]
Description=Run chat backend retention pruning hourly

[Timer]
OnCalendar=hourly
# Randomised so pruning does not happen on a predictable wall-clock edge.
RandomizedDelaySec=5m
Persistent=true
Unit=chat-maintenance.service

[Install]
WantedBy=timers.target
